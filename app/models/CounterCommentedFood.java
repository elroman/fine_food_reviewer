package models;

public class CounterCommentedFood extends AbstractCounterMessages {
    public CounterCommentedFood(String productId, int commentCounter) {
        super(productId, commentCounter);
    }
}
